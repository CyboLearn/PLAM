import { generatePageMeta } from "@/app/seo/generate";

export const metadata = generatePageMeta({
  title: "Personal Large Action Model",
  url: "/dashboard"
});

export default function Home() {
  return (
    <main>
    </main>
  );
}
